#operadores aritméticos - +(adição), -(subtração), *(multiplicação), /(divisão), **(exponenciação), //(divisão interna), %(módulo)
#operadores de atribuição - =(x = 1), +=(x += 1 => x = x + 1), -=(x -= 1 => x = x - 1), *=(x *= 1 => x = x * 1), /=(x /= 1 => x = x / 1), %=(x %= 1 => x = x % 1)
#operadores de comparação - >(maior), <(menor), ==(igual), !=(diferente), >=(maior ou igual), <=(menor ou igual)
#operadores lógicos - and, or, not
#operadores de identidade(compara se os objetos são idênticos) - is, is not
#operadores de associação(objeto está contido no conjunto?) - in, not in

#tipos primitivos(não precisa declarar) - str(string), int, float, bool(boolean), complex

#ação

#seleção
